{{#if isModalOpened}}
	<div class="{{classes.overlay}}" >
		<div class="{{classes.content}}" id="{{contentContainerId}}">
			<h4 class="{{headingClasses}}" >
				{{{title}}}
			</h4>

			{{{content}}}
		</div>
	</div>
{{/if}}
