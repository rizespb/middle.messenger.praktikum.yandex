<div class="{{classes.container}}" >
	{{#if label}}
		<label class="{{classes.label}}" for="{{name}}" >
			{{{label}}}
		</label>
	{{/if}}

	{{{input}}}

	{{#if error}}
		<span class={{classes.error}} >
			{{{error}}}
		</span>
	{{/if}}
</div>
